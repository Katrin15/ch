import { GrabImagePipe } from './grab-image.pipe';

describe('GrabImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GrabImagePipe();
    expect(pipe).toBeTruthy();
  });
});

import { ResolvePreviewImageUrlPipe } from './resolve-preview-image-url.pipe';

describe('ResolvePreviewImageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolvePreviewImageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
